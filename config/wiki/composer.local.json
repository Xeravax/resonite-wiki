{
    "extra": {
        "merge-plugin": {
            "include": [
                "extensions/*/composer.json",
                "skins/*/composer.json"
            ],
            "merge-dev": false
        }
    },
    "config": {
        "audit": {
            "comment": "affects test libraries (php unit), that we dont use in prod",
            "ignore": ["PKSA-z3gr-8qht-p93v"]
        }
    }
}
